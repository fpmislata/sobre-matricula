[pytest]
testpaths = tests
markers =
    unit: lógica pura, sin efectos secundarios
    integration: requiere mocks de LLM o filesystem
    slow: tests que tardan (PDF real, YOLO/Haar real)
addopts = -v --tb=short
